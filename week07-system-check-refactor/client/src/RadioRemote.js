import React, { Component } from 'react';
import logo from './logo.svg';
import './App.css';
///this is a copy of radioLocal

class RadioRemote extends Component {

    constructor(props) {
        super(props);
        this.dataEndPoints = {
           '/ssh-runner/'

    };
    handleRemote = event => {

    };

    runScript = (path + script).then.....

    handleChange = event => {
        const selectedValue = event.target.value;
        const endPointIndex = event.target.getAttribute('data-endpoint')
    };
    this.
    setState
....
}


render() {
    return (

        //radioWeb
        <div className="App">


            <main>
                <section>{radioRemote}</section>

            </main>
            <footer>
                <p>&copy; by Charlie Calvert </p>
            </footer>
        </div>
    );
}
}

export default RadioRemote;
